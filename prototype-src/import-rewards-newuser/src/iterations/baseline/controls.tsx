// REPLACE: Export a FineTuning component with { state, onChange } props.
// Build controls for continuous values (sliders), toggles, and selections
// that let the user explore the component's state space.
